import DarkModeButton from "./DarkModeButton";

export default function AppSettings() {
  return (
    <div>
      <label className="text-lg font-medium mb-4 block">App Settings</label>
      <DarkModeButton />
    </div>
  );
}
